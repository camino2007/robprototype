package fup.prototype.robprototype.details;

import android.support.annotation.NonNull;
import android.support.constraint.ConstraintLayout;

public class RepoItemHandler {

    public void onClick(@NonNull final RepoItemViewModel viewModel, @NonNull final ConstraintLayout constraintLayout) {
        /*final Context context = constraintLayout.getContext();
        final User user = viewModel.getUser();
        final Intent intent = DetailsActivity.createIntent(context, user);
        context.startActivity(intent);*/
    }
}
